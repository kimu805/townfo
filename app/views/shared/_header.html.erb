<header class="header">
  <div class="app_title">townfo</div>
  <ul class="nav_lists">
    <li class="nav_list"><%= current_group.name %></li>
    <li class="nav_list"><%= link_to current_user.nickname, group_user_path(group_id: current_group.id, id: current_user.id) %></li>
    <li class="nav_list">
      <%= link_to "ログアウト", destroy_user_session_path, data: {turbo_method: :delete}, class: "link" %>
    </li>
    <% if current_group && current_user.id == current_group.owner_id %>
      <li class="nav_list">
        <%= link_to "ユーザー管理", group_memberships_path(current_group), class: "link" %>
      </li>
    <% end %>
  </ul>
</header>